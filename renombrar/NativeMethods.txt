// Lista auditable de P/Invokes. Todas son APIs publicas documentadas y actuan sobre el
// propio proceso o el propio HWND. Cada grupo lleva encima para que es: ahi es donde
// mira un auditor, y cada entrada tiene que poder senalarse a una seccion de
// SEGURIDAD.md §3.
//
// Crece un grupo por hito. Si una entrada no se puede senalar a una seccion, o sobra la
// entrada o falta la seccion.

// --- Consola estando compilado como WinExe (H0) ---
// Sin esto, --previa y --check no tendrian donde escribir al lanzarse desde una terminal.
AttachConsole

// --- Orden de las filas (H1, SEGURIDAD.md §3.2) ---
// La comparacion que usa el Explorador para ordenar: numeros como numeros, foto2 antes
// que foto10. Es una funcion de comparar cadenas de shlwapi, no toca el disco. Esta aqui
// en vez de escrita a mano porque el orden de las filas es el orden en el que {n} reparte
// la numeracion, y lo que el usuario espera es lo que ve en la ventana de al lado.
StrCmpLogicalW

// --- La ventana propia (H3, SEGURIDAD.md §3.1) ---
// Una ventana normal y corriente. No hay nada aqui que mire, mueva ni toque una ventana
// ajena: todo actua sobre el HWND propio, que es lo que deja la regla 14 sin excepciones.
RegisterClassExW
CreateWindowExW
DefWindowProcW
DestroyWindow
PostQuitMessage
GetModuleHandleW
ShowWindow
SetWindowPos
LoadCursorW
IDC_ARROW
GetClientRect
GetDpiForWindow

// --- Bucle de mensajes propio (H3) ---
GetMessageW
TranslateMessage
DispatchMessageW

// --- Composition sobre el HWND propio (H3) ---
ICompositorDesktopInterop
CreateDispatcherQueueController
DispatcherQueueOptions

// --- Superficie donde se dibuja la tabla (H3) ---
// Fuera de XAML no existe LoadedImageSurface: para pintar texto hay que pasar por
// D3D11 -> D2D -> Composition. Este device sube pixeles, no renderiza frames.
D3D11CreateDevice
D2D1CreateDevice
ID2D1Device
ID2D1DeviceContext
ID2D1SolidColorBrush
IDXGIDevice
ID3D11Device
D3D_DRIVER_TYPE
D3D11_CREATE_DEVICE_FLAG
D2D1_ROUNDED_RECT
ICompositionDrawingSurfaceInterop
ICompositorInterop

// --- El texto de la tabla (H3) ---
DWriteCreateFactory
IDWriteFactory
IDWriteTextFormat
IDWriteTextLayout
DWRITE_FACTORY_TYPE
DWRITE_TEXT_METRICS
DWRITE_FONT_WEIGHT
DWRITE_FONT_STYLE
DWRITE_FONT_STRETCH
DWRITE_WORD_WRAPPING
D2D1_DRAW_TEXT_OPTIONS

// --- Recibir lo que se suelta (H3, SEGURIDAD.md §3.1) ---
// La UNICA entrada del programa. Es puramente receptivo: solo ve lo que el usuario suelta
// encima de esta ventana, con su gesto. No es el portapapeles, que sigue prohibido por la
// regla 9. De lo soltado solo se saca la ruta; el fichero no se abre.
OleInitialize
OleUninitialize
RegisterDragDrop
RevokeDragDrop
IDropTarget
SHCreateShellItemArrayFromDataObject
IShellItemArray
IShellItem
SIGDN


// --- Los controles de la franja (H4, SEGURIDAD.md §3.5) ---
// Cajas de texto y botones del sistema, hijos del HWND propio. Son controles de verdad
// para que el caret, la seleccion, las teclas muertas y Ctrl+V los haga el control y no
// este programa: aqui NO se leen teclas (regla 10), se le pregunta al control que pone.
SendMessageW
SetWindowTextW
GetWindowTextW
GetWindowTextLengthW
EnableWindow
SetWindowTheme
SystemParametersInfoForDpi
SYSTEM_PARAMETERS_INFO_ACTION
NONCLIENTMETRICSW
CreateFontIndirectW
DeleteObject

// Recalcular cuando dejas de teclear, no en cada tecla.
SetTimer
KillTimer

// Los controles pintan su propio fondo, y el suyo es claro: esto es lo que evita los
// parches blancos sobre la franja oscura.
CreateSolidBrush
DwmSetWindowAttribute
DWMWINDOWATTRIBUTE

// La banda de los controles la pinta el WndProc: Composition se compone por encima
// del HWND y taparia los controles hijos.
FillRect
